{
  // An explorer must run un-namespaced (RFC 09 §5): a session namespace would
  // hide exactly the foreign traffic zenctl exists to show. Refused by name.
  mode: "client",
  namespace: "zensight",
}
