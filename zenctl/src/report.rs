//! Typed reports — re-exported from the engine (issue #34).
//!
//! The structs moved to `zenkey_fleet::report` so zenctl and zengui render
//! one contract. This module keeps zenctl's internal seam name.

pub use zenkey_fleet::report::*;
